<!DOCTYPE html>
<html>
  <head>
    <title>Edit Product</title>
    <link
      href="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100">
    <div class="max-w-lg mx-auto mt-10 bg-white p-8 rounded shadow">
      <h1 class="text-2xl font-bold mb-6">Edit Product</h1>
      <form
        action="/products/<%= product._id %>?_method=PATCH"
        method="POST"
        class="space-y-4"
      >
        <input
          type="text"
          name="name"
          value="<%= product.name %>"
          class="w-full border p-2 rounded"
          required
        />
        <input
          type="number"
          name="warranty"
          value="<%= product.warranty %>"
          class="w-full border p-2 rounded"
          placeholder="Warranty (months)"
          required
        />
        <input
          type="number"
          name="price"
          value="<%= product.price %>"
          class="w-full border p-2 rounded"
          placeholder="Price"
          required
        />
        <input
          type="text"
          name="description"
          value="<%= product.description %>"
          class="w-full border p-2 rounded"
          placeholder="Description"
        />
        <label class="flex items-center">
          <input type="checkbox" name="isAvailable" <%= product.isAvailable ?
          "checked" : "" %> />
          <span class="ml-2">Available</span>
        </label>
        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">
          Update
        </button>
      </form>
      <a href="/products/view" class="text-blue-500 mt-4 inline-block"
        >Back to List</a
      >
    </div>
  </body>
</html>
