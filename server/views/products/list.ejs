<!-- filepath: d:\Uni\Sharda\Sem 6\microservices\Ecommerce\server\views\products\list.ejs -->
<!DOCTYPE html>
<html>
  <head>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Product List</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      /* ...existing styles... */
    </style>
  </head>
  <body>
    <!-- Product Cards Grid Start -->
    <div class="flex flex-wrap justify-center gap-8 my-8">
      <% if (products && products.length > 0) { %> <%
      products.forEach(function(product) { %>
      <div
        class="bg-white rounded-lg shadow-md overflow-hidden max-w-sm w-full"
      >
        <div class="relative">
          <img
            src="https://via.placeholder.com/400x300"
            alt="Product image"
            class="w-full h-64 object-cover"
          />
          <% if (product.isAvailable) { %>
          <span
            class="absolute top-2 left-2 bg-green-500 text-white text-xs font-bold px-2 py-1 rounded"
            >IN STOCK</span
          >
          <% } else { %>
          <span
            class="absolute top-2 left-2 bg-gray-500 text-white text-xs font-bold px-2 py-1 rounded"
            >OUT OF STOCK</span
          >
          <% } %>
          <button
            class="absolute top-2 right-2 bg-white p-2 rounded-full shadow-md hover:bg-gray-100 transition-colors duration-200"
          >
            <svg
              class="w-4 h- text-center text-blue-500"
              viewBox="0 0 24 24"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <g id="SVGRepo_bgCarrier" stroke-width="0"></g>
              <g
                id="SVGRepo_tracerCarrier"
                stroke-linecap="round"
                stroke-linejoin="round"
              ></g>
              <g id="SVGRepo_iconCarrier">
                <path
                  d="M15.7 4C18.87 4 21 6.98 21 9.76C21 15.39 12.16 20 12 20C11.84 20 3 15.39 3 9.76C3 6.98 5.13 4 8.3 4C10.12 4 11.31 4.91 12 5.71C12.69 4.91 13.88 4 15.7 4Z"
                  stroke="#000000"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                ></path>
              </g>
            </svg>
          </button>
        </div>
        <div class="p-4">
          <div class="flex justify-between items-start mb-2">
            <div>
              <h2 class="text-xl font-semibold text-gray-800 mb-1">
                <%= product.name %>
              </h2>
              <p class="text-sm text-gray-600">
                Warranty: <%= product.warranty %> months
              </p>
            </div>
          </div>
          <div class="py-2">
            <p class="text-lg font-bold text-green-600">
              ₹<%= product.price %>
            </p>
            <!-- You can add a line-through price if you have an "old price" field -->
          </div>
          <p class="text-gray-600 text-sm mb-4">
            <%= product.description || "No description provided." %>
          </p>
          <div class="flex items-center justify-between mb-4">
            <div class="flex items-center">
              <i class="fas fa-calendar-alt text-blue-500 mr-2"></i>
              <span class="text-sm text-gray-600">
                Added: <%= product.dateCreated ?
                product.dateCreated.toDateString() : "N/A" %>
              </span>
            </div>
            <div class="flex items-center">
              <i class="fas fa-check-circle text-blue-500 mr-2"></i>
              <span class="text-sm text-gray-600">
                <%= product.isAvailable ? "Available" : "Unavailable" %>
              </span>
            </div>
          </div>
          <div class="flex space-x-2">
            <a
              href="/products/<%= product._id %>"
              class="flex-1 bg-blue-500 text-white py-2 px-4 rounded-full font-semibold hover:bg-blue-600 transition-colors duration-200 text-center"
            >
              View
            </a>
            <a
              href="/products/<%= product._id %>/edit"
              class="bg-gray-200 text-gray-800 py-2 px-4 rounded-full font-semibold hover:bg-gray-300 transition-colors duration-200 text-center"
            >
              Edit
            </a>
          </div>
        </div>
      </div>
      <% }); %> <% } else { %>
      <p class="text-center w-full">No products found.</p>
      <% } %>
    </div>
    <!-- Product Cards Grid End -->

    <div style="text-align: center">
      <a href="/products/new">Add New Product</a>
    </div>
  </body>
</html>
